# Sentiment API dependencies. Ollama must be installed and running separately (see README).
# Python 3.8+; 3.12/3.13 recommended; 3.14 supported (pydantic 2.12+).
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
pydantic>=2.12.0
httpx>=0.27.0
python-dotenv>=1.0.1
pandas>=2.2.0